[pytest]
filterwarnings =
    ignore:'crypt' is deprecated and slated for removal in Python 3.13:DeprecationWarning:passlib.utils
markers =
    schema_reset: Requiere reconstruir el esquema completo (sin transacciones anidadas) antes y después de la prueba.
asyncio_mode = auto
