import {
  runBackup,
  triggerSync,
  listSyncOutbox,
  retrySyncOutbox,
  getSyncOutboxStats,
  getSyncHistory,
} from "../../../api";

export const syncService = {
  runBackup,
  triggerSync,
  listSyncOutbox,
  retrySyncOutbox,
  getSyncOutboxStats,
  getSyncHistory,
};
